import { eq } from "drizzle-orm";
import { getDb } from "../../../../../db";
import { companySettings,paymentTransactions,quoteItems,quotes } from "../../../../../db/schema";
const dummyBank="XYZ Bank\nAccount name: RobeFlow Wardrobes\nAccount number: 00-0000-0000000-00\nReference: Use your invoice number";
export async function GET(_:Request,context:{params:Promise<{token:string}>}){const value=(await context.params).token,db=getDb(),[quote]=await db.select().from(quotes).where(eq(quotes.acceptanceToken,value)).limit(1);if(!quote||!quote.invoiceSentAt)return Response.json({error:"Invoice not found."},{status:404});const quoteLines=await db.select().from(quoteItems).where(eq(quoteItems.quoteId,quote.id)).orderBy(quoteItems.sortOrder),items=[...quoteLines,...(quote.servicePrice>0?[{id:-1,category:quote.serviceType,systemType:"Service",colour:"",price:quote.servicePrice}]:[])],payments=await db.select().from(paymentTransactions).where(eq(paymentTransactions.quoteId,quote.id)),[settings]=await db.select().from(companySettings).where(eq(companySettings.id,1)).limit(1);return Response.json({invoice:{invoiceNumber:quote.invoiceNumber,quoteNumber:quote.quoteNumber,customerName:quote.customerName,companyName:quote.companyName,email:quote.email,siteAddress:quote.siteAddress,project:quote.project,amount:quote.amount,invoiceSentAt:quote.invoiceSentAt,items,payments,bankDetails:settings?.bankDetails.trim()||dummyBank,company:settings?.companyName||"RobeFlow Wardrobes",address:settings?.address||"Christchurch, New Zealand",gstNumber:settings?.gstNumber||""}})}
