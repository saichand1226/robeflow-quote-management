"use client";

import Link from "next/link";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";

export default function RegisterPage(){const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [message,setMessage]=useState("");const [error,setError]=useState("");async function register(e:React.FormEvent){e.preventDefault();setError("");const {error}=await createClient().auth.signUp({email,password,options:{data:{full_name:name,requested_role:"Sales"},emailRedirectTo:`${location.origin}/auth/callback`}});if(error){setError(error.message);return}setMessage("Check your email to verify the account. An administrator must approve access before you can use the staff workspace.")}return <main className="auth-wrap"><form className="auth-card" onSubmit={register}><Link className="brand" href="/"><span className="mark">R</span>RobeFlow</Link><h1>Request staff access</h1><p>Create your account. Access remains pending until an administrator approves it.</p><div className="field"><label htmlFor="name">Full name</label><input id="name" required value={name} onChange={e=>setName(e.target.value)}/></div><div className="field"><label htmlFor="email">Email address</label><input id="email" type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label htmlFor="password">Password</label><input id="password" type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></div>{error&&<div className="message error">{error}</div>}{message&&<div className="message">{message}</div>}<button className="btn primary">Create account</button><div className="small"><Link href="/login">Back to login</Link></div></form></main>}
