import { requireStaff } from "@/lib/api-auth";
export async function GET(){const a=await requireStaff();if(a.error)return a.error;const{data}=await a.supabase.from("quotes").select("*").eq("archived",false),today=new Date(),soon=new Date(today.getTime()+7*86400000),reminders:any[]=[];for(const q of data??[]){const valid=new Date(`${q.valid_until}T00:00:00`);if(["Draft","Sent"].includes(q.status)&&valid<=soon)reminders.push({id:`q-${q.id}`,quoteNumber:q.quote_number,customerName:q.customer_name,message:valid<today?"Quote has expired.":"Quote expires within 7 days.",urgency:valid<today?"Overdue":"Soon",type:"Quote",date:q.valid_until});if(q.status==="Accepted"&&q.invoice_sent_at&&!['Paid in Full','Account'].includes(q.invoice_status)){const issued=new Date(q.invoice_sent_at);if((today.getTime()-issued.getTime())/86400000>=7)reminders.push({id:`i-${q.id}`,quoteNumber:q.quote_number,customerName:q.customer_name,message:"Invoice remains unpaid.",urgency:"Overdue",type:"Invoice",date:q.invoice_sent_at.slice(0,10)})}}return Response.json({reminders})}
