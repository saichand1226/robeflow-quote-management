import Link from "next/link";
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";

export default async function Dashboard(){const supabase=await createClient();const{data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const{data:profile}=await supabase.from("staff_profiles").select("full_name,role,status").eq("id",user.id).maybeSingle();if(!profile||profile.status!=="Approved")return <main className="shell"><div className="dashboard"><Link className="brand" href="/"><span className="mark">R</span>RobeFlow</Link><section className="dash-card"><h1>Account awaiting approval</h1><p>Your email has been verified. An administrator must approve your staff account before the internal workspace becomes available.</p><p><strong>{user.email}</strong></p></section></div></main>;return <main className="shell"><div className="dashboard"><div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><Link className="brand" href="/"><span className="mark">R</span>RobeFlow</Link><span className="eyebrow">{profile.role}</span></div><section className="dash-card"><h1>Welcome, {profile.full_name||user.email}</h1><p>The secure Supabase session and role profile are working. The operational dashboard is being moved into this Vercel application in the next migration stage.</p></section></div></main>}
